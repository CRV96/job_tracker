/**
 * Applications: the core domain. Job applications, their status, and the event timeline of each one.
 * Owns the {@code applications} and {@code application_events} tables. Depends on identity.
 * <p>
 * Public API: the types in this package. Sub-packages are internal to the module.
 */
package com.jobtracker.applications;
