spring.application.name=job_tracker

# The schema is owned by the Flyway migrations in each module; Hibernate only checks that entities match it.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
