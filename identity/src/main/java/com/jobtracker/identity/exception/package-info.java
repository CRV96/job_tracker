/**
 * Exceptions the service throws. Part of the module's public API, so callers can handle them.
 */
@NamedInterface("exception")
package com.jobtracker.identity.exception;

import org.springframework.modulith.NamedInterface;
