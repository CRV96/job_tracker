/**
 * {@link com.jobtracker.jobs.service.ApplicationService} is the module's entry point for other modules.
 * {@code propagate = false} keeps the {@code impl} sub-package internal.
 */
@NamedInterface(name = "service", propagate = false)
package com.jobtracker.jobs.service;

import org.springframework.modulith.NamedInterface;
