/**
 * Web: the browser UI. Server-rendered pages (Thymeleaf + HTMX), the shared layout and the Tailwind build.
 * Calls the other modules only through their public APIs and holds no business logic. No other module depends on it.
 */
package com.jobtracker.web;
